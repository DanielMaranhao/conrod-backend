import { DocsForbiddenMapper } from './docs-forbidden.mapper';

describe('DocsForbiddenMapper', () => {
  it('should be defined', () => {
    expect(new DocsForbiddenMapper()).toBeDefined();
  });
});
