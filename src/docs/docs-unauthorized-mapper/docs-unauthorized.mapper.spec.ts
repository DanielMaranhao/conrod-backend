import { DocsUnauthorizedMapper } from './docs-unauthorized.mapper';

describe('DocsUnauthorizedMapper', () => {
  it('should be defined', () => {
    expect(new DocsUnauthorizedMapper()).toBeDefined();
  });
});
